# Aurum Signal 项目协作指南

## 1. 文档用途

本文件适用于 `D:\codex\project\demo2` 整个目录，供后续开发者和自动化编码代理快速理解项目并安全开展工作。

项目名称为 **Aurum Signal**，是一套本地运行的黄金市场趋势分析 Web 应用。它采集黄金、利率、美元、股票、商品和货币市场数据，通过规则模型生成未来 5 个交易日的方向判断、信号强度、观察区间、驱动因素、数据质量和历史验证结果。

当前产品方向和信息架构较完整，但模型仍属于启发式规则系统，不应宣称已经具备稳定预测能力，也不能把“信号强度”解释为预测正确概率。

## 2. 强制安全规则

### 2.1 禁止批量或递归删除

禁止使用以下命令：

```text
del /s
rd /s
rmdir /s
Remove-Item -Recurse
rm -rf
```

需要删除文件时，只能一次删除一个明确路径的文件，例如：

```powershell
Remove-Item "C:\path\to\file.txt"
```

如果任务需要批量删除文件或目录，必须停止操作并请求用户手动处理。

### 2.2 操作前说明

在运行命令或修改文件前，先向用户简要说明：

- 计划运行的命令或检查；
- 预计读取或修改的文件；
- 可能产生的数据写入、服务重启或其他副作用。

### 2.3 凭据和敏感信息

- 不读取 `.env`、密钥、Token、Cookie、私有凭据或类似文件。
- 不在日志、API 响应、测试输出或文档中暴露敏感信息。
- 不把本地绝对路径、原始异常文本或调用栈直接返回给前端。

### 2.4 高影响操作

未经用户明确确认，不执行：

- 删除业务数据；
- 发布或部署；
- 数据迁移；
- 推送远程仓库；
- 会覆盖历史记录的批量重算；
- 其他不可逆操作。

### 2.5 保护现有工作

- 工作区可能包含用户尚未提交的修改，不得擅自还原或覆盖。
- 修改前先查看 `git status --short`，只处理当前任务涉及的文件。
- 不使用 `git reset --hard`、`git checkout --` 等破坏性命令。
- 手工编辑文件应使用 `apply_patch`。
- 除非任务明确要求刷新市场数据，否则不要改写 `data/latest.json` 和 `data/history.json`。

## 3. 技术栈与运行方式

项目刻意保持轻量：

- 后端：Python 标准库；
- HTTP 服务：`ThreadingHTTPServer`；
- 网络请求：`urllib`；
- 前端：原生 HTML、CSS、JavaScript；
- 测试：`unittest`；
- 数据存储：本地 JSON；
- 无数据库、无前端构建步骤、无第三方 Python 依赖。

启动本地服务：

```powershell
python server.py
```

默认访问地址：

```text
http://127.0.0.1:8765/
```

执行一次完整分析并退出：

```powershell
python server.py --refresh
```

注意：`--refresh` 会访问外部数据源，并写入 `data/latest.json` 和 `data/history.json`。

运行全部测试：

```powershell
python -m unittest discover -s tests -v
```

## 4. 项目目录

```text
demo2/
├─ AGENTS.md                  项目协作与实现说明
├─ README.md                  基础介绍和启动说明
├─ config.json                时区、预测周期、人工政策输入和事件配置
├─ server.py                  HTTP 服务、缓存、并发合并和实时行情任务
├─ gold_app/
│  ├─ __init__.py
│  ├─ http_client.py          HTTP 请求、重试和 FetchError
│  ├─ market_data.py          市场数据与实时行情采集
│  ├─ scoring.py              规则评分、区间、质量和指标解释
│  ├─ history.py              预测记录、结算和验证指标
│  └─ service.py              数据采集、评分、历史和文件写入编排
├─ prototype/
│  ├─ index.html              单页应用结构
│  ├─ app.js                  数据渲染、交互和实时行情轮询
│  ├─ styles.css              页面样式和响应式布局
│  └─ mock-analysis.json      API 不可用时的演示数据
├─ docs/
│  └─ data-contract.md        JSON 和 API 数据契约
├─ data/
│  ├─ latest.json             最近一次成功分析
│  └─ history.json            每日预测及历史验证记录
└─ tests/                     单元、服务、接口、并发和容错测试
```

## 5. 核心处理流程

完整分析流程如下：

```text
config.json
    │
    ├─ market_data.collect_market_data()
    │      ├─ Yahoo 日线行情
    │      ├─ Gold API 现货金
    │      └─ U.S. Treasury 利率
    │
    ├─ scoring.build_analysis()
    │      ├─ 四大评分因子
    │      ├─ 参考系一致性
    │      ├─ 观察区间
    │      ├─ 指标解释
    │      └─ 数据质量与新鲜度
    │
    ├─ history.settle_history()
    ├─ history.add_today_prediction()
    └─ 原子写入 latest.json 和 history.json
```

实时报价是独立轻量流程：

```text
server.py 后台线程或 GET /api/quotes
    └─ market_data.collect_live_quotes()
           ├─ XAU/USD 现货报价
           └─ COMEX GC=F 五分钟行情
```

实时报价只更新内存缓存，不重新执行评分，也不写入历史文件。

## 6. 市场数据

### 6.1 外部来源

- Yahoo Finance Chart API：期货、ETF、股票指数、美元、商品和外汇；
- Gold API：XAU/USD 现货黄金报价；
- U.S. Treasury：美国实际收益率和名义收益率 XML 数据；
- 手工治理数据：`config.json` 中的美联储政策和央行购金分数。

`http_client.py` 为外部请求统一设置 User-Agent，并在失败时进行有限重试。对外 API 不得直接暴露 `FetchError` 的原始内容。

### 6.2 Yahoo 标的映射

| 内部键 | Yahoo 代码 | 用途 |
|---|---|---|
| `gc` | `GC=F` | COMEX 黄金连续主力 |
| `gld` | `GLD` | 黄金 ETF 量价代理 |
| `sp500` | `^GSPC` | 标普 500 |
| `nasdaq` | `^IXIC` | 纳斯达克综合指数 |
| `vix` | `^VIX` | 波动率指数 |
| `dxy` | `DX-Y.NYB` | 美元指数 |
| `wti` | `CL=F` | WTI 原油 |
| `silver` | `SI=F` | 白银期货 |
| `copper` | `HG=F` | 铜期货 |
| `eurusd` | `EURUSD=X` | 欧元兑美元 |
| `gbpusd` | `GBPUSD=X` | 英镑兑美元 |
| `audusd` | `AUDUSD=X` | 澳元兑美元 |
| `usdjpy` | `JPY=X` | 美元兑日元 |
| `usdcad` | `CAD=X` | 美元兑加元 |

### 6.3 缺失值约定

- 数据不足必须使用 `None`/JSON `null`，不得用 `0` 冒充真实数据。
- 单个观测不能计算 5 日变化，应返回缺失。
- 前值为零时不能计算百分比变化。
- 负数或非有限波动率视为无效输入。
- 前端格式化 `null` 时必须显示 `--`，不能显示 `0.00`。

### 6.4 Treasury 容错

实际收益率和名义收益率来自两个独立接口，必须分别获取和处理错误。任一接口失败时，另一个成功结果仍应保留。

## 7. 五分钟实时报价

主页的 XAU/USD 和 COMEX GC 报价通过 `GET /api/quotes` 更新：

- 默认轮询间隔为 300 秒；
- 页面首次加载立即请求；
- 页面重新变为可见时立即请求；
- 服务端使用内存缓存和 single-flight 合并并发请求；
- 后台线程只在金属市场交易时段主动刷新；
- 不触发完整模型采集，不写 `latest.json` 或 `history.json`。

COMEX 交易时段按纽约时间近似处理：

- 周六休市；
- 周日 18:00 ET 开市；
- 周五 17:00 ET 收市；
- 周一至周四每日 17:00–18:00 ET 为维护休市时段。

夏令时边界在代码中计算，不依赖系统 `tzdata`。

COMEX tooltip 可展示：

- 昨日收盘价；
- 当日开盘价；
- 当日最高价和最低价；
- 涨跌额和涨跌幅；
- 市场状态；
- 更新时间；
- 数据来源。

XAU/USD 是 OTC 市场，没有统一官方收盘价。现有 Gold API 不提供可靠的昨日收盘、日内开高低，因此这些字段保持 `null`，界面需明确说明，不能伪造。

盘中成交量不能直接与完整交易日均量比较。当前逻辑在 `marketState == REGULAR` 时不生成完整日成交量相对值，避免把半日成交量误判为缩量。

## 8. 评分模型

当前版本：

- Schema：`1.1.0`
- 模型：`rules-v1.2.0`
- 默认预测周期：未来 5 个交易日

模型输出属于规则评分和市场情景提示，不是投资建议，也不是经过统计校准的预测概率。

### 8.1 方向阈值

综合分理论范围为 `-100` 到 `100`：

| 综合分 | 方向 |
|---|---|
| `>= 40` | 强烈看多 |
| `>= 15` | 谨慎看多 |
| `-15 < score < 15` | 中性 |
| `<= -15` | 谨慎看空 |
| `<= -40` | 强烈看空 |

任何新增评分逻辑都必须保持因子和总分边界，不能让异常配置突破范围。

### 8.2 四大因子

| 因子 | 权重 | 主要输入 |
|---|---:|---|
| 美联储政策与利率 | 50 | 实际收益率、名义收益率、美元指数、人工 Fed 分数 |
| 股票与避险情绪 | 10 | 标普 500、纳斯达克、VIX |
| 黄金市场量价动能 | 30 | GLD 与 GC 的价格趋势和完整日成交量 |
| 央行购金 | 10 | 人工央行分数 |

“黄金市场量价动能”不能命名为“ETF 与期货资金流”。GLD/GC 成交量不等于 ETF 净申购、资金净流入、期货净多仓或未平仓合约变化。

### 8.3 人工分数边界

- 美联储人工分数限制在 `-5..5`；
- 央行人工分数限制在 `-10..10`；
- 无效、过期或无法解析的人工输入按 0 处理；
- `updated_at` 和 `expires_at` 必须参与新鲜度与过期判断。

### 8.4 黄金敏感方向

指标不能统一按照“上涨即利多黄金”解释。每个指标必须使用自己的 `gold_sensitivity` 或信号函数：

| 指标 | 对黄金的常用关系 |
|---|---|
| GC、GLD | 同向 |
| 实际收益率 | 反向 |
| 名义收益率 | 反向 |
| 美元指数 | 反向 |
| 标普 500、纳斯达克 | 模型中按避险逻辑反向 |
| VIX | 同向 |
| EUR/USD、GBP/USD、AUD/USD | 同向参考 |
| USD/JPY、USD/CAD | 反向参考 |

原油、白银和铜仅作为跨市场参考，解释时要保留其关系并非稳定因果关系的提示。

### 8.5 参考系

五项参考系用于判断跨市场一致性和调整信号强度，不直接修改原始综合分。参考一致率较低时应显示背离；严重背离路径必须保持可达且有测试覆盖。

### 8.6 信号强度

页面应使用“信号强度”或“模型一致度”，不要把它称作经过校准的“置信概率”。

当前启发式公式综合考虑：

- 字段级完整度；
- 综合分绝对值；
- 跨市场一致性奖励；
- 数据错误惩罚；
- 高风险事件惩罚。

兼容字段 `confidence` 可能仍存在，但必须同时标注 `is_probability: false`。数据严重缺失时，方向应降级为“数据不足”，信号强度上限应降低。

### 8.7 观察区间

观察区间基于 20 日波动率、预测周期和事件乘数生成：

- 无效波动率使用保守默认值；
- 波动率必须取有效非负值；
- 高风险事件会扩大区间；
- 输出是启发式观察范围，不是统计置信区间；
- 未积累足够历史样本前，`historical_coverage` 保持为空。

## 9. 数据质量与新鲜度

完整度必须按字段有效值计算，不能只检查键是否存在。

不同数据频率使用不同有效期：

| 数据 | 默认有效期 |
|---|---:|
| 现货黄金 | 24 小时 |
| Yahoo 日线行情 | 72 小时 |
| Treasury 数据 | 120 小时 |
| Fed 人工配置 | 14 天 |
| 央行人工配置 | 45 天 |

数据质量输出应包含：

- 字段级完整度；
- 错误数量和摘要；
- 延迟输入数量；
- 各来源新鲜度；
- 最旧核心数据时间；
- 总体状态。

`market_as_of` 主要代表黄金基准时间，不等于所有输入都同样新鲜。页面优先展示“最旧核心数据时间”，避免把局部新数据误报为全局新鲜。

## 10. 配置与治理

`config.json` 主要包含：

- 时区 `Asia/Shanghai`；
- 预测周期；
- 是否启用历史；
- 美联储人工政策分数；
- 央行购金人工分数；
- 重要事件；
- 计划接入的数据源。

人工输入至少应保留：

- 分数和状态；
- 来源名称及链接；
- 数据期；
- 更新时间；
- 到期时间；
- 维护人；
- 调整理由。

当前配置包含 2026 年 6 月 FOMC 事件和对应政策说明。这类内容具有时效性，修改或新增市场事件时必须核对绝对日期、时区和官方来源，不得长期把已经过去的事件保留为未来风险。

计划中的增强数据：

- CFTC 管理基金净多仓；
- GLD 份额或持仓吨数变化；
- COMEX 未平仓合约；
- 期货期限结构和展期影响；
- 自动化央行购金数据。

## 11. 历史记录与验证

`history.py` 管理每日预测记录、到期结算和模型验证。

每条预测必须固化：

- 模型版本；
- 预测周期；
- 方向评价阈值；
- 起始价格；
- 基准品种和合约类型；
- 是否经过换月调整；
- 因子和指标输入快照；
- 生成时间。

历史结算必须使用记录创建时保存的评价规则，不能用当前默认规则重新解释旧预测。

异常历史记录必须逐条隔离：

- 起始价为 0 时跳过；
- 区间缺失或格式错误时跳过相应计算；
- `outcome` 缺失时安全初始化；
- 单条坏记录不能阻断整个刷新；
- 文件写入必须采用临时文件替换的原子方式。

默认方向评价规则：

- 5 个交易日后涨幅 `>= +0.5%`：看多；
- 跌幅 `<= -0.5%`：看空；
- 其余：中性。

历史报告不应只显示准确率，还应包括：

- 已结算样本量；
- 类别分布；
- 混淆矩阵；
- 平衡准确率；
- 区间覆盖率；
- 校准准备状态。

至少积累 100 个具有可比性的样本后，才适合开始讨论概率校准。更严格的研究应优先使用非重叠样本。

当前历史基准是 Yahoo `GC=F` 连续主力：

- 未进行换月调整；
- 不是固定合约；
- 预测起点可能是盘中报价；
- 结果使用后续交易日收盘价；
- 因此不应宣称为严格的“结算价到结算价”验证。

## 12. HTTP API

### `GET /api/health`

返回服务健康状态。

### `GET /api/analysis`

返回最近一次分析结果。服务会优先使用内存或磁盘缓存，并用当前运行时配置补充事件和治理信息。

### `POST /api/refresh`

执行完整数据采集、评分、历史结算和文件写入。此接口有明显副作用，测试或调试前要说明。

多个并发刷新必须通过 condition/single-flight 合并，不能顺序执行多次完整采集。

### `GET /api/quotes`

返回 XAU/USD 和 COMEX GC 的五分钟实时报价。使用内存缓存，并合并并发请求，不写磁盘。

### 静态资源

其他路径从 `prototype/` 提供。路径解析必须限制在静态目录内，避免目录穿越。

### API 错误约定

对外错误应使用稳定、概括性的公共消息，例如“刷新失败，请稍后重试”。不得包含：

- Python 异常类型；
- 本地绝对路径；
- 外部接口完整错误正文；
- 调用栈；
- 内部实现细节。

## 13. 前端行为

前端是原生单页应用，主要页面包括：

- 总览；
- 驱动因素；
- 事件；
- 数据；
- 历史。

主要交互：

- 首次加载请求 `/api/analysis`；
- API 不可用时才使用 `mock-analysis.json`；
- 手动刷新按钮调用 `POST /api/refresh`；
- XAU/USD 和 COMEX GC 每五分钟请求 `/api/quotes`；
- 价格变化触发轻量视觉提示；
- 行情卡支持鼠标悬停和键盘聚焦 tooltip；
- 因子卡可打开参考系抽屉；
- 历史页支持筛选和统计展示。

前端修改要求：

- 保持中文文案和现有视觉体系；
- `null`、`undefined`、`NaN` 不得被格式化为零；
- tooltip 必须可键盘访问；
- 不重复出现“未来 未来5个交易日”等文案；
- 页面和 mock 数据的 Schema 版本必须与 API 契约一致；
- mock 只能作为降级演示数据，不能掩盖真实 API 契约回归；
- 指标利多/利空标签必须使用后端提供的黄金敏感方向，不能仅看涨跌符号。

## 14. 数据契约与版本

权威契约文档为 `docs/data-contract.md`。

修改 JSON 字段、枚举、含义或 API 行为时，应同步检查：

- `gold_app/scoring.py`；
- `gold_app/service.py`；
- `server.py`；
- `prototype/app.js`；
- `prototype/mock-analysis.json`；
- `docs/data-contract.md`；
- 相关测试。

兼容性原则：

- 新增字段优先保持向后兼容；
- 变更字段语义时更新 Schema 版本；
- 缓存中的旧模型快照要明确标识，不能静默伪装成新模型结果；
- 前端不得依赖没有记录在契约中的偶然字段；
- 时间统一使用带时区的 ISO 8601 字符串。

## 15. 测试要求

现有测试覆盖：

- 评分边界和方向阈值；
- 人工分数限幅与过期；
- 字段缺失导致的质量降级；
- 负波动率；
- 指标黄金敏感方向；
- 数据新鲜度；
- 事件区间乘数；
- 历史规则固化；
- 异常历史记录隔离；
- 历史验证指标；
- 单观测序列；
- Treasury 独立容错；
- 盘中成交量处理；
- 五分钟报价解析和交易时段；
- 分析刷新并发合并；
- 报价缓存和并发合并；
- API 错误脱敏；
- 缓存分析的运行时配置覆盖。

新增功能时按风险补充测试。优先测试：

- 数据缺失、空值和异常类型；
- 时间边界、时区和夏令时；
- 并发请求；
- 外部接口部分失败；
- 文件原子写入；
- API 契约；
- 前端缺失值；
- 模型分数边界；
- 历史规则不可变性。

测试应使用 mock 或固定样本，避免依赖实时网络和当前市场状态。文档类修改可以不跑完整测试，但至少执行格式检查；代码修改应运行相关测试，并在影响共享行为时运行全部测试。

## 16. 编码约定

- 延续现有 Python 标准库实现，除非新增依赖有明确收益并获得用户同意。
- 保持函数职责清晰，优先复用现有 helper 和数据结构。
- 金融数值解析必须防御 `None`、字符串、零除、非有限值和负数异常。
- 结构化数据使用 JSON/API 解析，不使用脆弱的字符串截取。
- 文件写入优先采用临时文件加 `replace` 的原子模式。
- 并发共享状态使用现有锁和 condition 模式。
- 新增缓存时必须明确 TTL、失效条件和失败回退。
- 注释用于解释金融口径、边界条件和不明显的并发逻辑，不重复描述代码表面行为。
- 文件统一使用 UTF-8；代码标识符优先使用 ASCII。
- 不做与当前任务无关的重构或格式化。

## 17. 已知模型限制

开发和文案中必须诚实保留以下限制：

- 固定阈值和固定权重尚未经过充分历史校准；
- 信号强度不是预测正确概率；
- 观察区间不是统计置信区间；
- GLD 和 GC 成交量不等于真实资金流或持仓；
- 尚未正式纳入 CFTC、ETF 实际持仓、未平仓合约和期限结构；
- 连续期货存在换月影响；
- 盘中起点与后续收盘结果口径不完全一致；
- 市场事件和跳空风险只做了有限启发式调整；
- `market_as_of` 不代表所有输入的最旧时间；
- 样本不足时不能据此评价稳定准确率、概率校准或区间覆盖能力。

## 18. 推荐迭代顺序

1. 持续保证评分边界、字段级完整度、频率化新鲜度和前端缺失值正确。
2. 完善事件日历、人工政策输入治理和最旧核心数据展示。
3. 接入 CFTC、ETF 实际持仓、COMEX 未平仓合约和期限结构。
4. 统一历史预测与结算口径，处理固定合约或连续序列换月调整。
5. 积累足够可比样本后，报告混淆矩阵、平衡准确率和区间覆盖率。
6. 达到校准样本要求后，再研究概率输出、动态阈值、动态权重和市场状态分层。

## 19. 修改完成检查清单

提交结果前至少确认：

- 没有读取凭据或执行禁止命令；
- 没有覆盖用户无关改动；
- 没有把缺失金融数据变成 0；
- 没有突破评分边界；
- 没有把信号强度描述为概率；
- 没有泄露本地路径或原始异常；
- API、前端、mock 和数据契约保持一致；
- 相关测试已经执行并说明结果；
- 若运行过 `--refresh` 或 `/api/refresh`，已明确说明数据文件副作用。
